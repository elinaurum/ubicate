# Dependencias de ejecución. Fijar versiones exactas antes de desplegar
# a producción (ver docs/DESPLIEGUE.md §2).
streamlit>=1.50
folium>=0.16
pydantic>=2.6
pydantic-settings>=2.2

# Proveedor de modelo.
# openai: cliente para OpenAI y para los endpoints compatibles — Gemini y Groq
# (ver ADR-0005). Es el que usa el despliegue actual.
openai>=1.30
# anthropic: solo si se cambia UBICATE_PROVEEDOR_LLM a "anthropic".
# anthropic>=0.40
